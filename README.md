Build projektu:
gradle clean build

Uruchomienie clienta w konsoli:
gradle :client:run --console=plain --no-configuration-cache

Uruchomienie klienta w gui:
gradle :client:run --console=plain --args="gui" --no-configuration-cache

SETNAME -> CREATE ROOM -> ( JOIN ) -> PICK COLOR -> BEGIN -> ROZGRYWKA...

move row col
